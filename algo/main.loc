module bio.algo (*)

import conventions (Str, Real, Int, List, Unit)

import bio.tree (pullNode, Tree)
import bio.pure

source Cpp from "algo.hpp" ("nj", "align")

nj :: [Str] -> Tree () Real Int
align :: [Str] -> [Str]
