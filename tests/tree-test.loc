import treeTest (foo)

-- import cppbase (id)
import rbase (id)
import bio.tree (Tree, randomTree)

foo Cpp :: "int" -> "Tree<$1,$2,$3>" "std::string" "double" "std::string"
foo n = id (randomTree n)
