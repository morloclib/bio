module treeTest (foo)

import conventions (Str, Real)

-- functions for language conversion
import rbase (id as asR)
import pybase (id as asPy)
import cppbase (id as asCpp)

import bio.tree (Tree, randomTree)

foo n = asR . asCpp . asPy . randomTree
